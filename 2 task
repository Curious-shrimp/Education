a = float(input("Введите первое число: ")) #Лучше делать нормальный ввод, изменил первое число, как пример
b = float(input("Введите второе число"))
c = float(input("Введите третье число"))
d = float(input("Введите четвертое число"))
sum1 = a + b
sum2 = c + d     # Можно записать в одну строчку, но не критично (sum1, sum2 = a + b, c + d)
result = sum1 / sum2
print(result)
    # При некоторых делениях могут быть ебанутые дроби, можно записать print(round(result, 1)
    # 1 показыает до какого числа после запятой округлять, 0 или отсутствие аргумента - до целого

    # Если ты точно знаешь, что в ответ нужно целое число
    # (с дробными компуктер работает ебано,
    # а обычное деление ВСЕГДА возвращает тип float), то можно взять int(result)